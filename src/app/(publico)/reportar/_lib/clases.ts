/** La clase global `.input` más un anillo de foco con el token `pozo`. */
export const CLASE_INPUT =
  "input min-h-11 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-pozo";

/**
 * Clases del Boton primario de Vía 3.
 * Se copian en el `<a href="tel:">` porque Boton no acepta `as` todavía.
 */
export const CLASE_BOTON_PRIMARIO =
  "inline-flex min-h-11 min-w-11 items-center justify-center gap-2 rounded-xl border-l-4 border-l-pila bg-pozo px-4 text-base font-semibold text-white transition hover:bg-chorro focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-pozo motion-reduce:transition-none";
